- name: hello-world
  query: 
    SELECT * FROM UNNEST([(100, -1, 'a', null, true, 1.23), (2, 0, 'bravo', 1, false, -2/3)]);
- name: param
  query: SELECT * FROM UNNEST([(@name, @id)]);
  parameters:
    id: FLOAT
    name: STRING